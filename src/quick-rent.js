function decorateCards(){document.querySelectorAll('.card').forEach(card=>{if(card.querySelector('.quick-action'))return;let borrowed=card.innerText.includes('대여 중'),b=document.createElement('button');b.className='quick-action '+(borrowed?'return':'borrow');b.textContent=borrowed?'↩ 반납하기':'↗ 대여하기';b.onclick=e=>{e.stopPropagation();quickRental(card,borrowed)};card.querySelector('.body').appendChild(b)})}function quickRental(card,borrowed){let name=card.querySelector('h3').textContent;if(borrowed){alert(name+'이(가) 반납 처리되었습니다.');return}document.body.insertAdjacentHTML('beforeend',`<div class="quick-modal" id="quick-modal"><form class="quick-form"><button type="button" class="quick-close">×</button><h2>놀잇감 대여하기</h2><p>${name}의 사용 정보를 입력해주세요.</p><label>사용 반<input required placeholder="예: 무지개반"></label><label>반납 예정일<input type="date" required></label><button class="button">대여 등록</button></form></div>`);let modal=document.getElementById('quick-modal');modal.querySelector('.quick-close').onclick=()=>modal.remove();modal.querySelector('form').onsubmit=e=>{e.preventDefault();modal.remove();let s=card.querySelector('.status');if(s){s.textContent='대여 중';s.classList.add('borrowed')}let a=card.querySelector('.quick-action');a.textContent='↩ 반납하기';a.classList.replace('borrow','return')}}new MutationObserver(decorateCards).observe(document.getElementById('app'),{childList:true,subtree:true});decorateCards();
